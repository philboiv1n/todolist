# Copy to `private_todo/.env` and adjust for your environment.
#
# Admin hardening: allow only these IPs/CIDRs to access admin pages.
# Example: TODO_ADMIN_IP_ALLOWLIST="1.2.3.4, 5.6.7.0/24, ::1"
# TODO_ADMIN_IP_ALLOWLIST=
#
# How long to keep failed login attempts (seconds). Default: 300 (5 minutes).
# Max: 2592000 (30 days).
# TODO_LOGIN_ATTEMPTS_RETENTION_SECONDS=86400
#
# Optional session directory override (must be writable).
# TODO_SESSION_SAVE_PATH=/home/username/private_todo/sessions
#
# Optional SQLite tuning knobs:
# TODO_SQLITE_BUSY_TIMEOUT_MS=5000
# TODO_SQLITE_JOURNAL_MODE=WAL
# TODO_SQLITE_SYNCHRONOUS=NORMAL

